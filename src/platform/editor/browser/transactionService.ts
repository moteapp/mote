import { Disposable } from 'mote/base/common/lifecycle';
import { Transaction } from '../common/transaction';

export class TransactionService extends Disposable {

    public async applyTransaction(transaction: Transaction) {
        
    }
}