export enum DropType {
    None = '',
    Block = 'block',
    Menu = 'menu',
    Relation = 'relation',
    Record = 'record',
    Widget = 'widget',
}

export enum SelectType {
    None = '',
    Block = 'block',
    Record = 'record',
}
