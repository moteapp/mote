spec:
  replicas: 1
  template:
    spec:
      containers:
      - name: controller
        resources:
          requests:
            cpu: "20m"
            memory: "156Mi"
          limits:
            cpu: "150m"
            memory: "200Mi"
